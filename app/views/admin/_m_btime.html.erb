            <div class="well well-out">
                <form action="/admin/m_btime/<%= @b.id %>" method="POST">
                    <input type="hidden" name="mod" value="<%= @mod %>">
                    <input type="hidden" name="id" value="<%= @b.id %>">
                    <div class="well well-in">
                        <div class="form-group2">
                            <label>이름</label>
                            <input type="text" name="name" class="form-control" value="<%= @b.name %>" required/>
                        </div>
                        <div class="form-group2">
                            <label>min</label>
                            <input type="number" name="min" class="form-control" value="<%= @b.min %>" required/>
                        </div>
                        <div class="form-group2">
                            <label>max</label>
                            <input type="number" name="max" class="form-control" value="<%= @b.max %>" required/>
                        </div>
                    </div>
                
                    <div class="row" align="right">
                        <% if @mod == "1" %>
                            <a class="btn btn-orange" href="/admin/del_btime?id=<%= @b.id %>">삭제</a>
                            <input type="submit" value="수정" class="btn btn-orange" />
                        <% else %>
                            <input type="submit" value="등록" class="btn btn-orange" />
                        <% end %>
                        <a class="btn btn-orange" href="/admin/index?admin=btime">취소</a>
                    </div>
                </form>
                
            
            
            </div>
        </div>
        
    	<div class="row">
    		<hr class="feature-divider">
    	</div>
    </div>
	<div class="col-md-1"></div>
</div>