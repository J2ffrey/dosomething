<script>

function refresh(year,month,day){
				var site_text = "/calendar/large?" + 'month=' + month
  			+ '&year=' + year
  			+ '&day=' + day;
			
				$('#large').load(site_text);
}

$(function(){
  
  $('#prev_').on('click', function() {
					var site_text = "/calendar/large?" + 'month=' + "<%= @month1 %>"
  			+ '&year=' + "<%= @year1 %>"
  			// + '&nocol=1'
  			+ '&day=' + "<%= @day1 %>";
				
				$('#large').load(site_text);
				});
  })
  
$(function(){
  $('#next_').on('click', function() {
				var site_text = "/calendar/large?" + 'month=' + "<%= @month2 %>"
  			+ '&year=' + "<%= @year2 %>"
  			// + '&nocol=1'
  			+ '&day=' + "<%= @day2 %>";
			
				$('#large').load(site_text);
			});
			
})

</script>